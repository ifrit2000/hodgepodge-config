spring:
  redis:
    host: 172.28.0.2
    port: 6379
    database: ${redis.database}
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 10000ms
